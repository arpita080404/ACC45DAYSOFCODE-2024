#include <bits/stdc++.h>
using namespace std;

int main() {
	int T;
	cin>>T;
	while(T--){
	    int N;
	    cin>>N;
	    int A[N];
	    for(int i=0;i<N;i++){
	        cin>>A[i];
	    }
	    int d=0;
	    for(int i=0;i<N;i++){
	       if(A[i]!=0)
	       d=i;
	    }
	    cout<<d<<endl;
	}
}
